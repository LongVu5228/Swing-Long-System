// This Pine Script™ code is subject to the terms of the Mozilla Public License 2.0 at https://mozilla.org/MPL/2.0/
// Original recreation inspired by the publicly described feature set of "TraderLion's Enhanced Volume"
// (source code for that script is closed/proprietary; this is an independent implementation from scratch).
//@version=6
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//// INDICATOR
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
indicator(title = 'Enhanced Volume', shorttitle = 'EVol+', overlay = false, format = format.volume)

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//// INPUTS
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
ev_ma_len = input.int(
     defval = 30,
     minval = 1,
     title  = 'Volume MA Length',
     group  = 'Volume Coloring'
 )

ev_up_day_color = input.color(
     color.white,
     'Up Day Volume Color',
     group   = 'Volume Coloring',
     tooltip = 'Used on green (close >= open) days.'
 )

ev_down_day_color = input.color(
     #ff0000,
     'Down Day Volume Color',
     group   = 'Volume Coloring',
     tooltip = 'Used on red (close < open) days. Dark red rather than a bright red'
 )

ev_border_color = input.color(
     #4D4D4D,
     'Bar Outline Color',
     group   = 'Volume Coloring',
     tooltip = 'Outline drawn around every volume bar'
 )

ev_ma_color = input.color(color.new(#9598A1, 20), 'Volume MA Color', group = 'Volume Coloring')

ev_scale_headroom_pct = input.float(
     defval  = 20,
     minval  = 0,
     maxval  = 100,
     step    = 5,
     title   = 'Scale Headroom Above Bars (%)',
     group   = 'Volume Coloring',
     tooltip = 'Reserves empty space above the tallest bar so TradingView\'s floating pane buttons (move/eye/remove/settings) do not sit on top of the volume bars'
 )

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//// VOLUME COLORING
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
float vol_ma = ta.sma(volume, ev_ma_len)

bool is_green_day = close >= open
color bar_color = is_green_day ? ev_up_day_color : ev_down_day_color

plotcandle(0, volume, 0, volume, title = 'Volume', color = bar_color, wickcolor = ev_border_color, bordercolor = ev_border_color)
plot(vol_ma, title = 'Volume MA', color = ev_ma_color, linewidth = 1)

float scale_ceiling = volume * (1 + ev_scale_headroom_pct / 100)
plot(scale_ceiling, title = 'Scale Headroom (invisible)', color = color.new(color.white, 100), display = display.pane)

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//// END
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
